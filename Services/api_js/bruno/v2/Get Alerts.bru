meta {
  name: Get Alerts
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v2/alerts
  body: none
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

docs {
  Get all alerts (V2 - camelCase).

  Returns alerts with camelCase field:
  - itemId
}
