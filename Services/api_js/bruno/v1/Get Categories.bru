meta {
  name: Get Categories
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/categories
  body: none
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

docs {
  Get all categories (V1).

  Note: Categories already use clean naming (no snake_case).
}
